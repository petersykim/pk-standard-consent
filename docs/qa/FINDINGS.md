# pk-standard-consent QA Findings

| # | Plugin | Dimension | Sev | Finding | Fix commit | Regression test |
|---|--------|-----------|-----|---------|------------|-----------------|
| C-001 | pk-standard-consent | D1 Functional | S3 | CORRECTION (2026-07-10): the original finding was FALSE. It claimed `clearLog()` was "imported and called NOWHERE" -- but it looked only at `ConsentLog.tsx`. `clearLog()` IS wired: `Settings.tsx` has a Danger-zone "Clear log" button (`DangerPanel`, line 142) -> a destructive `ConfirmDialog` ("Permanently deletes all stored consent records", line 61) -> `handleClearLog` (line 38) -> `clearLog(config)`. So the whole-log wipe has a real, reachable admin UI on the Settings screen; it is NOT dead code and NOT a missing control. The agent audited the wrong screen. | RESOLVED (2026-07-10, Peter): remove the whole-log Clear-log UI, keep scoped per-visitor erase as the only log-deletion path. Removed the Danger-zone button/handler/ConfirmDialog from `Settings.tsx` and the now-unused `clearLog()` client fn from `api/log.ts`; rebuilt `assets/dist/admin.js` (commit cfdd12c). The DELETE /admin/log REST route is left in place (harmless, still REST-tested) but has no UI trigger. | Removal guarded by `consent-settings-toggles.spec.ts` ("Danger zone keeps the uninstall control but has no whole-log Clear-log button", commit fa4af03); whole-log REST wipe still covered by `consent-admin-log-and-categories.spec.ts`. |
| C-002 | pk-standard-consent | Da11y Accessibility | S3 | The shared `@pk/ui` `<Switch>` component (`_shared/ui/src/components/Field.tsx`) accepted an `aria-label` prop at every consent admin call site (8 total: Settings.tsx x4, RegionsTable.tsx x3, CookieScan.tsx x1) but `SwitchProps` never declared it and the component never forwarded it to the underlying `<input>` -- every one of these switches had ZERO accessible name. Confirmed live: `getByLabel()` (which mirrors how a screen reader identifies a control) could not find any of them before the fix. Checked all 8 other plugins in the workspace for the same `<Switch aria-label=...>` pattern -- zero hits, so this is contained to consent, not a workspace-wide design-system bug. | fixed -- `SwitchProps` now declares `"aria-label"?: string`, `Switch` forwards it onto the `<input>`; consent's `assets/dist/admin.js` rebuilt via `pnpm build` to pick up the shared-component change | `e2e/qa/consent-bug-switch-aria-label.spec.ts` |
| C-003 | pk-standard-consent | D2 Visual | S3 | The opt-in banner's action buttons ("Manage preferences" / "Reject all" / "Accept all") overflowed the viewport by ~20-52px at a 360px mobile width, measured live via `getBoundingClientRect`. Two compounding causes: `.pk-sc-banner__inner` never wrapped its two children (`__copy`/`__actions`) onto separate rows (no `flex-wrap`), and `.pk-sc-banner__actions` had `flex-shrink: 0`, pinning it to its natural 3-buttons-unwrapped width (348px) at every viewport regardless of actual available space -- `flex-wrap` on a non-shrinking container never gets a chance to engage. | fixed -- `.pk-sc-banner__inner` gets `flex-wrap: wrap`; `.pk-sc-banner__actions` drops `flex-shrink: 0` for `max-width: 100%` | `e2e/qa/consent-bug-banner-mobile-overflow.spec.ts` |
| C-004 | pk-standard-consent | Dsec / D1 | S1 | CONSENT BYPASS (real-user audit 2026-07-15, verified live via `TagRewriter::rewrite`): a self-closing `<iframe .../>` or an unclosed `<iframe>` was NOT gated -- it passed through LIVE, ungated, from first page load with no consent. The iframe regex required `>(.*?)</iframe>`, which neither self-closing nor unclosed forms match. Both are valid, common copy-paste shapes (incl. Google Maps' own "Embed a map" code, a gated provider). A CMP that shows a banner while the tracker loads is the exact failure the plugin exists to prevent. | fixed (commit cb5df5b) -- `TagRewriter::rewrite` matches the opening iframe tag and OPTIONALLY consumes a body+close, tolerating a trailing slash (`#<iframe\b([^>]*?)\s*/?>(?:...)?#is`); rewriteIframe only used the attrs capture so the body/close requirement was dead weight. Verified: all four iframe shapes gate pre-consent, a non-gated iframe untouched (no over-gating), full existing gate suite green. | `e2e/qa/consent-bug-selfclosing-iframe-bypass.spec.ts` |
| C-005 | pk-standard-consent | Dsec | S3-open | A literal `>` inside a QUOTED attribute value before `data-pk-sc-category` (e.g. `<script data-config="a>b" data-pk-sc-category="analytics">`) truncates the `[^>]*` attrs capture in the SCRIPT regex, so the category attr is missed and the script bypasses gating. VERIFIED at the regex level against the real `TagRewriter::rewrite`, but UNCONFIRMED as a live-page path: WP's own content filters (wpautop) entity-decode a raw `>` in post content before it reaches TagRewriter; the live risk is limited to theme-header / GTM-container / raw-template scripts that carry a literal `>` in a config attr and skip wpautop. | OPEN / DEFERRED -- an attribute-aware regex (`(?:"[^"]*"|'[^']*'|[^>'"])*`) fixes it but needs careful verification against the full front-end (a first attempt tripped the accept-step gate re-injection under a loaded box); worth a deliberate fix once a real live bypass path is confirmed, not gold-plated under audit pressure. | (none yet — regex-level repro documented here) |
